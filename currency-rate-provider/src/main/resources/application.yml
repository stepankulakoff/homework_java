server:
  port: ${SERVER_PORT:0}
  shutdown: graceful

spring:
  application:
    name: currency-rate-provider
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  lifecycle:
    timeout-per-shutdown-phase: 30s
